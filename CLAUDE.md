# مقام — قواعد ملزمة لكل جلسة تنفيذ

اقرأ هذا الملف قبل أي تعديل. هذه قرارات معمارية مؤكَّدة من مالك المشروع، لا تُناقَش داخل الجلسة — إن بدت إحداها خاطئة، أوقف ونبّه بدل أن تتجاوزها.

## قواعد صارمة

1. **عقد البوابة الواحدة إلزامي لأي مستودع بيانات جديد.**
   كل مستودع جديد (Track, Beat, Take, Drill) يطبّق نفس نمط `BarRepository`:
   `query() / getById() / save() / subscribe?()`. ممنوع استيراد مصدر بيانات مباشرة داخل مكوّن React — يمر دائماً عبر `RepoProvider`/`useRepo()` أو مكافئه الجديد.

2. **كل كود جديد بـTypeScript صارم.** لا ملفات `.jsx` جديدة. الملفات القديمة (`StudioScreen.jsx` إلخ) تُترك كما هي ولا تُعاد كتابتها إلا إذا طُلب صراحة.

3. **كل وحدة تصل مع اختباراتها.** لا تُعتبر الدفعة منتهية إن نقص `npm test` أو `npm run typecheck` أو `npm run build` نظيفاً. الأساس الحالي ١٢٥ اختباراً ناجحاً — لا يجوز أن ينخفض هذا الرقم.

4. **الصوت في IndexedDB، النصوص في localStorage.** أي بيانات صوتية (Take blobs) تُخزَّن عبر Dexie، لا عبر localStorage. فعّل `DexieBarRepo`/ما يعادله عند دفعة الصوت فقط.

5. **مخطط واحد للتبادل مع Kaggle: `beat.v1.json`.** أي حقل جديد يرفع رقم الإصدار صراحة (`beat.v2.json`) ولا يُعدَّل الإصدار الحالي بأثر رجعي.

6. **لا ركن خامس.** الأركان الأربعة ثابتة: التدريب/المدرّب، الاستوديو/الكتابة، التحليل/الأداء، المستودع/الخزنة. أي ميزة جديدة تُركَّب كتبويب داخل أحدها.

7. **لا يُبنى محرك جديد يكرر وظيفة قائمة.** قبل إضافة أي منطق تحليلي، تحقق أولاً من `lib/syllabifier`, `lib/barRepo`, `features/dawra/engine`. إن كانت الوظيفة مغطاة، وسّع الموجود ولا تُضِف بديلاً.

8. **الهوية البصرية الرسمية عند العمل على أي واجهة:**
   خلفية بنفسجي داكن `#0B0710 → #1A0E1F` · ذهبي `#F5C518` · سماوي `#29ABE2` · Cairo 900 للعناوين · IBM Plex Mono للتسميات التقنية. لا "Techno-Arabic Brutalism" (ذهبي/أسود) في أي كود جديد.
   عزل الألوان: عائلات القافية `rA-rG` تبقى مستقلة تماماً عن ألوان الشارات `--c1..--c7` — ممنوع تكرار قيمة لونية بين المجموعتين.

## دورة العمل الملزمة لكل دفعة

1. اقرأ قسم الدفعة كاملاً من المواصفة الملصقة في الرسالة قبل أي تعديل.
2. اكتب خطة الملفات المتأثرة أولاً (بدون تنفيذ) وانتظر لحظة، ثم نفّذ.
3. نفّذ التعديلات + الاختبارات المطلوبة في العقد سوياً — لا تُسلَّم دفعة بلا اختباراتها.
4. شغّل `npm run typecheck && npm test && npm run build` قبل أي commit.
5. `git commit` بوصف عربي أو إنكليزي مختصر يذكر رقم الدفعة، مثال: `feat(batch-0): vault snapshot export/import + draft mode`.
6. توقف عند `git push` — هذا خارج نطاقك التلقائي، ينتظر تأكيداً صريحاً.
7. لا تنتقل لدفعة تالية ولا تُضف ميزة غير مطلوبة في الدفعة الحالية مهما بدت مفيدة.

## قاعدة التجميد

لا تُعرَّف أو تُبنى الـ٤٢ تقنية النواة ولا `TSpec X-Ray` حتى إشعار آخر. أي إشارة لهما في الكود أو التعليقات تبقى TODO موثّقاً فقط.
